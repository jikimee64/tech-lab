spring:
  datasource:
    driver-class-name: org.h2.Driver
    url: jdbc:h2:mem:lock
    username: sa
    password:
  data:
    redis:
      host: 127.0.0.1
      port: 6379

#logging:
#  level:
#    org.hibernate.SQL: DEBUG
#    org.hibernate.orm.jdbc.bind: TRACE